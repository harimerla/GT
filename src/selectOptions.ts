import React from 'react'
import Select from 'react-select'

export async function react_select_options(){
    var cancerType = document.getElementById('');

const options = [
  { value: 1, label: 1 },
  { value: 2, label: 2 },
  { value: 3, label: 3 }
]

const MyComponent = () => (
  <Select options={options} />
)
}